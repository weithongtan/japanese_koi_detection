import 'package:flutter/material.dart';

const GEMINI_API_KEY = "YOUR_GEMINI_API";

const bgColor = Color.fromARGB(255, 0, 0, 0);

const textColor = Color.fromARGB(255, 255, 255, 255);

const normalText = TextStyle(fontSize: 14, color: bgColor);

const titleNormalText = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);

const row1Padding = EdgeInsets.only( left: 20, right: 20);

const titleFont = TextStyle(fontSize: 30, fontWeight: FontWeight.bold, color: textColor);
